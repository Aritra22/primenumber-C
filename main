#include<stdio.h>
int main()
{
int n,count=0,i;
printf("Enter any Number");
scanf("%d",&n);
for(i=1,i<=n,i++)
{
if(n%i==0)
{
count++;
}
}
if(count==0)
printf("The Number is prime");
else
printf("The Number is Not Prime");
return 0;
}
